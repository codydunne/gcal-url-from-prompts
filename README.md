# gcal-url-from-prompts

Creates a link that generates a Google Calendar event on your calendar when you provide details at terminal prompts.

Run at the terminal with:

```bash
uv run --script .\gcal-url-from-prompts.py
```
